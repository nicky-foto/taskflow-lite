# No view crosses a workspace boundary

Every list, including My tasks, is scoped to one workspace, even for a user who is a Member of several.
A cross-workspace "everything assigned to me" view was requested and rejected for now: each workspace is a separate team's data,
and a single query that spans workspaces is the easiest place to leak one team's tasks to another.
